---
name: godot-personal-gotchas
description: Godot 4.x gotcha lookup — matches symptoms (silent failures, settings that don't take effect, mode setters that no-op, panels showing stale state) to a known cause and fix, plus a proactive pre-commit scan. Use on any Godot project (.gd, .tscn, .tres, project.godot, godot-mcp/godot-ai tools) when an operation behaves unexpectedly; BEFORE a risky operation — hand-editing a .tscn/.tres, changing window/display mode from script, `:=` with clamp/min/max/abs/sign, headless --script testing, exporting builds, mutating scenes via MCP; and before committing any Godot change (the pre-commit scan).
---

# Godot Personal Gotchas

A growing index of non-obvious Godot 4.x failures observed first-hand. **Consult by symptom, not by component** — most entries here look like "I set X, no error was thrown, but nothing changed."

## How to use

When something in Godot behaves unexpectedly, scan the index table by symptom **before** debugging from scratch. The point of this skill is that some of these failures have no error signal at all — you can't grep for them, you have to recognize them.

When no entry matches and you later find the cause, add the new gotcha here (see "Adding new gotchas" at the bottom).

> **Migration note:** a project's `docs/godot-gotchas.md` that still mirrors this index predates
> the single-source rule (see "Adding new gotchas") — offer to run `/audit-godot-parity`, which
> shrinks the project doc (removing verified duplicates) under parity-table approval.

## Proactive pre-commit scan

Before committing Godot changes, scan the diff against the **Detect proactively** line of each
relevant index entry (read the body file for the full pattern) — this skill is the single source
for those detection patterns. Highest-yield checks: `.tscn` ` = null` overrides (#3); `:=` on
`clamp`/`min`/`max`/`abs`/`sign` (#2) and on cross-script member access (#6); `window_set_mode`
from script (#1); non-code files under a docs folder without `.gdignore` (#7); `basis.z` without a
leading `-` (#17). Report `file:line → entry → fix`; silence is not a pass.

## Tooling: which MCP to use

Many entries below are godot-mcp write quirks — the meta-fix is **use the right tool for the job**:
- **godot-ai = WRITER** — all scene/node/script/property writes, `input_map_manage`, `project_run`, `editor_screenshot`, `logs_read`. Writes every struct type correctly.
- **godot-mcp = READ/TEST** — `godot_input` (inject timed actions, UNIQUE), `godot_runtime_state` (numerical pos/vel/rot), `godot_docs` (version-correct class docs, EXCLUSIVE), `godot_editor get_log_messages source="editor"` (parse errors — `get_errors`/`get_debug_output` were removed in v3.6.1), `godot_editor get_stack_trace` (crashes). **Never write through godot-mcp** (silently no-ops `Rect2` — see #15).
- **minimal-godot = local diagnostics** — `get_diagnostics` (line:col; misses cross-script Variant inference — cross-check `get_log_messages source="editor"`).

One writer per editor instance (both drive the same `EditorInterface`; a second editor instance — e.g. on a worktree — is a second independent writer).

## Gotcha index

| # | Symptom | Cause |
|---|---|---|
| 1 | `DisplayServer.window_set_mode(...)` or `get_window().mode = ...` silently no-ops — mode reads back unchanged, no error | Game is running embedded in the editor's Game tab |
| 2 | GDScript parse error "The variable type is being inferred from a Variant value" on a line like `var x := clamp(a, 0.0, 1.0)` | `clamp`/`min`/`max`/`abs`/`sign`/`floor`/`ceil`/`round` are Variant-returning globals; `:=` infers Variant; warnings-as-errors rejects it |
| 3 | After live Inspector tuning, `.tscn` gets `property = null` lines; on fresh load typed `@export` floats silently become `0.0` (Godot 4.6.x) | Inspector clear-override writes `property = null` instead of deleting the line; null coerces to `0.0` on 4.6.x scene load |
| 4 | AnimationTree dock: "double-click a node to enter sub-editor", "right-click → Rename", "Set as Start" all silently missing/no-op (Godot 4.6.2+) | AnimationTree dock UI reworked in Godot 4.6.x — the old affordances no longer exist |
| 5 | After incremental AnimationTree build, Output panel spams `Type mismatch float and bool` + `playback_new.is_null()` every frame while the scene is open; silent on other scenes; fires even with `active = false` | Editor's AnimationTree dock holds a stale preview cache against the newer sub-resource tree |
| 6 | GDScript parse error `Cannot infer the type of "x" variable because the value doesn't have a set type` on `var steering := _other.is_steering()` / `var slow := speed < _other.idle_threshold` | Source script lacks `class_name`, so its members resolve to Variant at parse time; also fires on an untyped param's member |
| 7 | Dropping a non-code file (`.svg`/`.png`/`.glb`) into a docs folder makes Godot generate a sibling `.import` and surface it in the FileSystem dock as a packageable resource | Godot scans the whole project root for importable files — no special-casing for docs/notes folders |
| 8 | AnimationTree StateMachine transition with `advance_condition`/`advance_expression` never auto-fires even when the condition parameter genuinely flips to `true`; `playback.get_current_node()` stays on the source state | `advance_mode` defaults to `ENABLED = 1` (travel-only); condition auto-fire needs `AUTO = 2`; editor dock authors the wrong default |
| 9 | Inside a `func(...)` lambda, reassigning a captured local `bool`/`int`/`float`/`String` from the enclosing scope silently does nothing — no parse error, no warning, lambda runs but outer scalar never updates | GDScript 4 closures capture outer scalars by value; the lambda mutates a private copy (`self.*`/Dictionary entries are reference-safe) |
| 10 | A semi-implicit Euler spring driving a node's `scale` (squash-stretch, hit feedback) blows up within ~10 frames of first impulse — rig becomes invisible (scale past ±10), physics still works, no error | At 60fps with `freq > ~6-7 Hz` the damping bound `2·zeta·omega·dt < 1` breaks before the freq bound; amplitudes grow each cycle |
| 11 | Walkthrough says set `bone_index` AND `bone2d_node` separately on a Skeleton2D modification; in 4.6.2 Inspector, picking the Bone2D from `bone2d_node` auto-populates `bone_index` — looks like 2 fields but it's 1 | Skeleton2D modification UI computes `bone_index` from the picked NodePath; both serialize but the integer is UI-derived, not user-input |
| 12 | GDScript parse error `Identifier "expf"/"sqrtf"/"sinf"/"powf"/"logf" not declared in the current scope` — not a return-type warning, the identifier simply does not exist | Godot 4 ships typed `*f`/`*i` variants only for a narrow comparison/rounding whitelist; transcendentals/trig are Variant-only (sibling-but-inverse: #2) |
| 13 | Headless Godot (GUT, `--check-only`, CLI) fails `Could not find type "X"` for a `class_name X` script that exists on disk; `mcp__godot__get_diagnostics` reports the file AND its consumer clean | `.godot/global_script_class_cache.cfg` doesn't refresh when a `class_name` file is created externally (Write, `cat`, Finder); editor LSP gives false-positive green, headless reads the stale cache |
| 14 | AnimationTree character stuck on Idle while clearly moving; a nested sub-StateMachine's `playback.get_current_node()` stays on Start/Idle; standing still, the top-level SM oscillates between two states every few frames; no errors/warnings | A boolean `advance_condition`/`advance_expression` set in only ONE state's branch latches stale-true, re-firing a spurious transition that resets the nested sub-SM to Start each frame (sibling: #8) |
| 15 | godot-mcp `node.update` silently no-ops `Rect2`/`region_rect` writes in every format (v3.6.1; `Transform2D` origin + `NodePath` were fixed in 3.6.1); after a hand-edit the open editor's stale in-memory copy makes `get_properties` lie and an editor save clobbers the hand-edit | The bridge's property-set path doesn't serialize `Rect2`, and the editor doesn't reload an externally-modified open scene without a close+reopen (sibling: #55) |
| 16 | A `RigidBody2D` pinned via `PinJoint2D` to an `AnimatableBody2D` anchor that is a child of a `move_and_slide()` `CharacterBody2D` stays frozen at spawn — won't ride the moving parent; also holds spawn Y on the initial gravity settle; no error/warning | `sync_to_physics = true` makes the `AnimatableBody2D` read its transform authoritatively from the physics frame, ignoring the parent's scene-tree transform update |
| 17 | A `Node3D` faces/moves backward — code computes "forward" as `+Z`, uses `transform.basis.z` directly, or `atan2(horizontal.x, horizontal.z)` for a heading | Godot's convention is **local -Z is forward** (`look_at` and `-transform.basis.z` both assume it); `+Z`-forward code fights the engine |
| 18 | Assigning an array LITERAL to a typed `Array[T]` PROPERTY throws at runtime `Invalid assignment of property 'x' with value of type 'Array'` even when elements match `T`; parses clean so `--check-only`/`get_diagnostics` miss it | An array literal `[...]` is an untyped `Array`; Godot 4 won't coerce it into a typed `Array[T]` property on assignment |
| 19 | godot-ai writes a script `[ext_resource]` line WITHOUT `uid=` after `node_create`+`script_attach`+`scene_save` on a brand-new script (resolves by `path=`, breaks on later rename/move) | The `.uid` sidecar doesn't exist yet at save time, so godot-ai has no UID to serialize |
| 20 | GDScript parse error `Cannot find member "TEXTURE_FILTER_INHERIT" in base "CanvasItem"` — a LOAD-time error (F5/boot) that GUT and a `[x]` docs-confirm checkbox both miss; cascades through any preloader | Godot 4.6's `CanvasItem.TextureFilter` has no `_INHERIT` member; the inherit/default value is `TEXTURE_FILTER_PARENT_NODE` (= 0) |
| 21 | Can't compile-check a standalone `.gdshader` headless — `godot --headless --check-only` (and any headless run) never surfaces shader syntax/type errors | Headless uses the dummy RenderingServer, which never compiles shaders; a `.gdshader` compiles only when a real RenderingServer loads it into a used material |
| 22 | godot-ai `resource_manage op=create`/`op=get_info` with `type="<YourClassName>"` (a `class_name X extends Resource`) fails `VALUE_OUT_OF_RANGE: Unknown resource type: X`; built-in types (`Curve`, `Environment`) work | godot-ai resolves type names against the engine's built-in ClassDB only, not the script `class_name` cache |
| 23 | godot-ai cannot create `Skeleton3D` bones — `node_set_property bones/0/name` → `PROPERTY_NOT_ON_CLASS`; `batch_execute` can't call `add_bone` either | Bones aren't nodes or settable properties (no `bones/N/*` until the bone exists) and godot-ai has no method-call verb (sibling: #25) |
| 24 | godot-ai `node_set_property` of a `Vector2i` (e.g. `SubViewport.size`) doesn't land — v2.5.x sets the container LENGTH (dict/array both → `Vector2i(2,2)`); v2.7.2–2.7.6 silently no-ops | godot-ai's value coercion had no `Vector2i`/`Vector3i`/`Vector4i` branch pre-2.8.0 (`Vector3` was always fine); sibling to godot-mcp `Rect2` no-op #15 |
| 25 | godot-ai cannot author an AnimationTree graph — `animation_manage` only does AnimationPlayer ops (no BlendTree/StateMachine/BlendSpace verbs); `add_property_track` is value-track-only (bone clips need `rotation_3d`/`position_3d` transform tracks) | godot-ai's animation surface is AnimationPlayer-only; the AnimationTree graph nodes and 3D transform tracks have no authoring verbs (sibling: #23) |
| 26 | Headless GDScript test (`SceneTree`+`_initialize`+`--script`): a node's `_ready()` does NOT fire synchronously after `add_child(node)` inside `_initialize()`; full-lifecycle assertions silently run against a not-yet-ready node | `_initialize` runs before the tree processes ready notifications |
| 27 | A headless `extends SceneTree` + `_initialize` + `--script` test exits 0 (green to `$?`) even on a PARSE failure or a mid-run RUNTIME abort | GDScript runtime errors don't propagate up the call stack, and a `--script` parse/load failure sets no nonzero exit — either bypasses `quit(1)` |
| 28 | A test pinning "this base is abstract / not instantiable" via `not script.can_instantiate()` is inverted — passes against a NON-abstract script, fails against the genuinely-abstract one; no error/warning | `can_instantiate()` reports whether the script is COMPILED/valid, not whether `.new()` is permitted; it returns `true` for an `@abstract` script (Godot 4.6.2/4.7) |
| 29 | Need to move/reorganize `.gd`/`.tscn`/`.tres` into new directories — godot-ai `filesystem_manage` has NO move op (only read/write/reimport/search); an out-of-editor `mv`/Write-to-new-path orphans every reference | Dependency-safe moves are an editor FileSystem-dock operation; godot-ai has no move verb and out-of-editor moves bypass the dependency-rewrite pass |
| 30 | Copying a `.gdextension` addon into `addons/` while the editor has the project open kills the editor instantly — no error dialog, no crash report; `.godot/extension_list.cfg` already contains the new entry | Server-level extensions (physics/rendering/audio) register only at the Servers-init startup stage and can't hot-load; the filesystem watcher's mid-session load attempt kills the process |
| 31 | A `--headless` run on macOS prints the engine banner then sits at 0% CPU forever — exit code never arrives, looks like an engine/GDExtension deadlock | Godot routes fatal setup errors through `OS_MacOS::alert()`, which runs a modal `NSAlert` `runModal` loop even headless (sibling: #27) |
| 32 | godot-ai `game_manage op=input_mouse event="button"` reports `sent:true` and `_unhandled_input` fires, but a handler using `get_global_mouse_position()` acts at ONE fixed far-off-window world position for every injected click; spawned bodies freefall like tunneling | `get_global_mouse_position()` reads the Viewport's tracked OS cursor (outside the window in MCP sessions), not the injected event's `position` |
| 33 | Rapier `Fluid2D` particles silently vanish through `StaticBody2D` walls (points at y~4e6, x~±30k); pool level stops rising; worse at smaller `fluid_particle_radius_2d`; also the addon `Faucet2D`'s stream | SPH boundary coupling is only kernel-radius thick (`fluid_smoothing_factor` × `fluid_particle_radius_2d`); per-step travel beyond it ejects the particle ballistically |
| 34 | After godot-ai `op=reimport` of a GDScript, `logs_read source="editor"` shows fatal-looking `Identifier not found: <AutoloadName>` or `Parse Error: ... has no resource loaders` — while tests pass and the game boots clean | Two transient EditorFileSystem races (dependent recompile mid-autoload-reload; reimport races `.import` sidecar generation); the editor log buffer is append-only and never retracts |
| 35 | `godot --headless --check-only --script <file.gd>` fails `Compile Error: Identifier not found: <AutoloadName>` for a script referencing a project autoload — though it's registered in `project.godot` and runs fine at F5/boot | `--check-only --script` parses one script without initializing the SceneTree, so `[autoload]` singletons are never registered as compile-time globals (sibling: #34) |
| 36 | godot-ai dock: "The server exited before the WebSocket handshake" on Intel/x86_64 macOS; a direct `uvx` run shows `Building cryptography==49.0.0` → `Failed to build` (`cargo metadata` errors on `package.authors`) | `cryptography==49` ships no x86_64-mac wheel so uv builds from source, but cargo 1.43 is too old to build it (sibling: #37) |
| 37 | godot-ai dock "exited before the WebSocket handshake" + `Building cryptography==49.0.0` / `Failed to build`, on **Apple Silicon (arm64)**; a native arm64 `uv` ALONE does not fix it | uv resolves wheels for the Python interpreter's arch, not uv's; an x86_64 Python pulls x86_64 wheels and cryptography≥49 has none → forced source build fails. (sibling: #36) |
| 38 | After godot-ai `script_attach`+`scene_save` on a **brand-new Write-tool `.gd`**, the FIRST saved `[ext_resource type="Script"]` has `path=` but no `uid=` (siblings have it) | The `.gd.uid` sidecar doesn't exist yet at first save and the FS watcher hasn't scanned the new file. (siblings: #13, #19) |
| 39 | A GDScript test `assert_eq(some_dict, Vector2i(3,4))` (or any `Rect2`/`Transform2D`/struct Variant) PASSES even when `some_dict` is a raw un-coerced `Dictionary` — a false green verifying nothing | `==`/`!=` between a `Dictionary` and an incompatible struct emits a non-fatal `Invalid operands` error evaluating to `false`, so `assert_eq` records no failure. (sibling: #28) |
| 40 | (godot-ai 2.8.1+, post-#583) `resource_manage op=create type="MyThing"` (or inline `{"__class__":"MyThing"}`) on a `class_name MyThing extends Resource` returns `WRONG_TYPE: … add @tool to instantiate it here` though `MyThing` is cache-registered | PR #583 gates create on `Script.can_instantiate()`, which is `false` for a non-`@tool` script in the editor. Correct Godot behavior. (successor to #22) |
| 41 | A held `godot_input` injection (`sequence`, long `duration_ms`) paired with a SAME-batch `source="game"` screenshot captures the AFTER-state (transient effect already released), never the during-hold visual; no error, the shot lands at the wrong moment | The injection holds the editor main thread for `duration_ms`; the same-batch screenshot is serviced only after the hold releases. (sibling: #42) |
| 42 | A group/population count read via `godot_runtime_state digest` `entity_count` reads SMALLER than the real group — looks like shrinkage / no growth when it genuinely changed; no error | `entity_count` is the RETURNED node count, capped by `max_nodes` (default 40), not the true group size; larger groups silently truncate. (sibling: #41) |
| 43 | godot-ai `source="game"` screenshot / `logs_read` / every `game_manage` runtime op fails `INTERNAL_ERROR: Game-side autoload never registered its debugger capture within 20s`; tell: `game_capture_ready` stuck false though `is_playing:true` | Game running embedded in the editor's Game tab — the embedded viewport never completes the `_mcp_game_helper` debugger-capture handshake, so capture never arms (siblings: #01, #41/#42) |
| 44 | GDScript `Cannot infer the type of "x"…` as a runtime Parser Error (at `preload`/`load`, NOT edit time) indexing an array LITERAL `["a","b"][i]`; blind to `--check-only` AND the LSP | Indexing an untyped array literal yields `Variant`, which `:=` can't infer; the script compiles only at load (siblings: #2/#6/#12, #18) |
| 45 | godot-ai `input_map_manage op=list` returns `[]` or only session-added/builtin actions — the project's own `project.godot` `[input]` actions (the WASD/fire actions the game uses) never appear, even with `include_builtin=true` | `list` reads the editor process's live `InputMap.get_actions()`, not `project.godot` `[input]` (those load only when the game runs) (sibling: #46) |
| 46 | godot-ai `input_map_manage op=bind_event event_type="key"` writes a LOGICAL `keycode` (`"keycode":N,"physical_keycode":0`), not the physical `"keycode":0,"physical_keycode":N` the editor UI and layout-independent WASD conventions use | bind_event's key path sets only the logical `keycode` and has no `physical_keycode` param; the editor UI records physical → tool/UI mismatch (sibling: #45) |
| 47 | `godot --headless` (even `--quit` with NO project) segfaults `signal 11` in `mvk::SPIRVToMSLConverter::convert` at rendering-device init inside the Claude Code sandbox on macOS; `--rendering-driver dummy`/`opengl3` and `arch -arm64`/`-x86_64` don't help | The command sandbox blocks the GPU/Metal access MoltenVK needs at headless boot, so it segfaults rather than degrading to the dummy renderer (siblings: #31, #27) |
| 48 | A `canvas_item` shader on a `QuadMesh` (`MeshInstance2D`) rebuilding local coords as `p = (UV - 0.5) * size` silently MIRRORS its output vertically vs CPU geometry sharing the "same" local frame (render must agree with a hit-test / SDF / ray-test); no error, only shows when you overlay render vs CPU | `QuadMesh` maps `UV.y=0` → local `+Y`, so `(UV.y-0.5) = -local_y` and `atan(p.y,p.x)` evaluates at `-theta`; `p.x` is fine. Headless-blind (#21) |
| 49 | A full-rect `Control` (`SubViewportContainer`, `stretch=true`) under a `Node2D` stays `size=(0,0)` despite the Full Rect preset — no error; the screen fills with one flat colour while a sibling `CanvasLayer` HUD renders fine | Full-rect anchors resolve against the parent's area, supplied only by a Control/CanvasLayer/Window/root viewport — a plain `Node2D` gives no rect → 0×0 |
| 50 | Raising `SubViewportContainer.stretch_shrink` to pixelate a 2D scene also zooms the camera view IN — framing tightens as the shrink rises, not just the pixel size; no error, it looks like the camera moved | `stretch_shrink` divides the SubViewport's pixel count by N; a `Camera2D` shows `viewport_size/zoom` world units, so fewer pixels = less world shown. (sibling: #49) |
| 51 | A headless `SceneTree` `--script` test working in `_initialize()` aborts `Cannot call method 'get_nodes_in_group' on a null value` (`get_tree()` null; `Parameter "data.tree" is null`); `add_to_group` there silently fails to register | During `SceneTree._initialize()` the root Window isn't in the tree yet, so children get `get_tree()==null` and group membership goes unregistered. (sibling: #26) |
| 52 | godot-ai `node_set_property` on a typed `Array[T]` export succeeds and `node_get_properties` reads it back populated, but the saved `.tscn` serializes an untyped `prop = [...]` that silently loads EMPTY — `node_get_properties` lies | The bridge sets an untyped `Array` (accepted in-memory); the saved bare `[...]` literal is rejected by the typed export at load. (siblings: #24, #18) |
| 53 | `Trying to assign invalid previously freed instance` fires on a line that just READS a stored ref into a TYPED local (`var t: Object = dict[k]`), even with an `is_instance_valid()` guard on the next line; `--check-only`/LSP miss it | A typed assignment validates the source against its declared type; validating a freed instance faults during the READ, before the guard runs |
| 54 | On macOS a Godot F-key binding (`physical_keycode == KEY_F9`/`F10`, or an Input-Map F-key action) silently never fires — the key triggers a system media action; injected keycodes (godot-ai `input_key`) DO fire it, testing GREEN under MCP | macOS's default "Use F1, F2… as standard function keys" is OFF, so the top row emits media events; F1–F12 reach the app only with Fn held |
| 55 | A hand-edit to an OPEN `.tscn`/`.tres`/`project.godot` (written from outside the editor) is correct on disk, then silently REVERTS later — node/action/value gone, no error; can wipe committed content mid-merge | Editor keeps stale in-memory copies of open resources, no auto-reload; its next save writes them back over the disk edit (siblings: #15/#24/#52) |
| 56 | After enabling `physics_interpolation` project-wide, things moving OUTSIDE the physics tick judder/streak — `_process` writer, idle-mode `Tween`, spawn-teleport (`add_child`+set-position), `_draw` overlay; `_physics_process` movers get smoother; no error, headless blind | Interpolation lerps physics-tick snapshots; any off-tick transform source renders against a stale one, and INHERIT default spreads it tree-wide |
| 57 | `ResourceSaver.save` of the SAME resource twice yields byte-DIFFERENT files (same length) — `diff` shows only a different random ExtResource id suffix (`id="1_yd20u"` vs `id="1_58qyq"`); breaks generate-then-byte-diff / regen-compare guards | `ResourceSaver` assigns each ExtResource a random 5-char suffix on its numeric id every save (Godot 4.4+ stable-id scheme) |
| 58 | Prepending a `# GENERATED …` marker line before `[gd_resource …]` in a `.tres` breaks loading (`Parse Error: Expected '['`, loads null, cascades through preloaders); common when stamping generated resources | Godot's text-resource parser requires `[gd_resource` as the first token; `#` is not a comment leader in this format (`;` is) |
| 59 | Benching GPU/frame cost on macOS Metal: `viewport_get_measured_render_time_gpu` returns 0.0 always; `Performance.TIME_PROCESS` isn't a wall clock even uncapped; sequential A/B splits swing run-to-run with p95 pinned at 16.67 ms | Three broken instruments: GPU timestamp queries unsupported on Metal; TIME_PROCESS includes engine waits; macOS occlusion throttling re-imposes pacing mid-run |
| 60 | `%g` in a GDScript format string throws `unsupported format character` at RUNTIME (per execution, e.g. every HUD frame); `--check-only`, tests, and preload-smoke are all blind — the string is data, not syntax | GDScript `String %` supports only `s c d o x X f v` — the C/printf `%g`/`%e` don't exist; the format string is data, evaluated only when the line runs |
| 61 | Zeroing an exported frequency/divisor knob (Inspector drag, `.tscn` override) makes the spring/filter-driven node FREEZE or VANISH — no error, no warning, anywhere; the sim keeps running | GDScript FLOAT division by zero is silent IEEE (`inf`/`0.0/0.0 = NaN`, only int div errors); constants like `zeta/(π·f)` NaN-poison the channel on the first update and a NaN transform component un-renders the node |
| 62 | First editor save of an older `.tscn` (even a one-node MCP add) rewrites the WHOLE file: `uid=` appears on header + script ext_resources, `unique_id=` stamps every node, and committed property lines VANISH from the diff — looks like corruption / lost overrides; no error | Godot 4.7 save-time serialization stamps scene/script UIDs + per-node `unique_id`, and ResourceSaver prunes any property equal to the script's CURRENT `@export` default — load-identical ONLY if each pruned value truly matches the default |
| 63 | `Input.MOUSE_MODE_HIDDEN` blanks the OS cursor across the WHOLE desktop while the game runs (macOS) — moving off the game window leaves you mousing blind; no error | `mouse_mode` is process-global and the hide isn't clipped to the window on macOS |
| 64 | A screen-space CanvasLayer (full-rect post-process ColorRect, overlay) silently draws BELOW a same-`layer` CanvasLayer elsewhere in the scene when it is nested under a plain Node — the effect just doesn't apply; no error; a green boot smoke passes while the look is wrong | Same-`layer` CanvasLayer ordering does not follow scene-tree DFS position once one is nested deeper under a non-Canvas node; only root-level/equal-depth placement orders predictably |
| 65 | Headless test: setting `.value` on an `HSlider`/`VSlider`/`SpinBox` that is OUTSIDE the SceneTree updates `.value` but silently never emits `value_changed` — the handler under test sees fired=0 while the value reads back changed; `CheckBox.toggled`/`Button.pressed` are NOT gated | `Range` routes its signal through a tree-dependent change pipeline, so an out-of-tree setter mutates silently; with #51 (no tree during `SceneTree._initialize`) a headless test can't attach to escape the gate |
| 66 | A non-dev export whose preset merely EXCLUDES `addons/` boots spamming `Failed to instantiate an autoload` SCRIPT ERRORs — the MCP autoloads (`MCPGameBridge`, `_mcp_game_helper`) ship broken despite the filter; dev builds are fine; the export command itself reports success | The vendored godot-mcp/godot-ai editor plugins force-re-add their game-side autoloads into `project.godot` on editor-enable AND mid-`--export` (headless export runs the editor binary, re-initing enabled plugins), and Godot force-includes autoload SCRIPTS in every export regardless of the resource filter — the bridge script ships without its filtered-out class deps |
| 67 | A previously-loading imported `.glb` starts failing `Resource file not found: <Stem>_<name>.png` / `Can't load dependency` after an asset-dir cleanup or mirror-sync ran; the glb is byte-identical and a re-run of `--import` "succeeds" without fixing anything | Godot's scene importer EXTRACTS embedded glTF images to `<Stem>_<name>.png` beside the source glb and the imported `.scn` depends on them; a mirror/prune tool sees the generated pngs as strays and deletes them, and re-import skips regeneration because the source glb's md5 is unchanged |
| 68 | Scroll-wheel input code (`MOUSE_BUTTON_WHEEL_UP/DOWN` in `_unhandled_input`) works in a web/wasm build and with a physical mouse, but two-finger trackpad scrolling on macOS does nothing — no event, no error | macOS trackpad scrolling arrives as `InputEventPanGesture` (phase-branched in `DisplayServerMacOS scrollWheel:` — pan OR wheel per event, never both); wheel-button handlers never fire for it. Browsers translate trackpad to DOM wheel, so wasm builds mask the gap |
| 69 | A new script/shader committed by explicit path (`git add <file>`, per a "never `git add <dir>/`" staging rule) boots fine locally but a fresh clone regenerates a DIFFERENT `uid://`, silently breaking `uid://` refs in tracked `.tscn`/`.tres` — because the `.gd`/`.gdshader` was staged and committed while its auto-generated `.uid` twin was left untracked | Godot 4.4+ writes a `.uid` sidecar per script/shader and bakes `uid://` (not path) into scene/resource refs; explicit-path staging names only the source file, so the sidecar never enters the commit, and a clone with no `.uid` mints a fresh one that doesn't match the committed refs |
| 70 | An input gate "while a text field has focus, swallow app hotkeys" (`gui_get_focus_owner() is LineEdit` → early return from `_input`) permanently kills EVERY hotkey after the user Escs out of the field — all keys dead until a stray click; no error, and the caret is gone so the field doesn't even look focused | Godot 4.4 split LineEdit focus from editing: Esc (`ui_cancel`) exits EDIT MODE but the control KEEPS keyboard focus, so a focus-owner test stays true forever — gate on `is_editing()`, not focus (bonus: synthetic test events must set BOTH `keycode` and `physical_keycode`, or built-in `ui_*` actions never match) |
| 71 | Web/HTML5 build: an `AudioEffectCapture` on Master captures hundreds of thousands of frames of pure SILENCE and `get_playback_position()` freezes after exactly one mix block (`0.0026666` s = 128 frames @48 kHz) — while the browser is genuinely making sound; no error anywhere, native runs fine | Godot 4.3+ defaults the web platform to `PLAYBACK_TYPE_SAMPLE`, which plays via the browser and bypasses Godot's bus graph entirely ("AudioEffects are not supported when playback is considered as a sample"), so engine-side meters are structurally blind, not failing; the `audio/general/default_playback_type.web` project setting does NOT override it — only a per-player `playback_type` does |
| 72 | Custom CLI flags passed to a `godot` run (`--set X=Y`, `--my-flag`) are SILENTLY invisible to the game — `OS.get_cmdline_user_args()` returns `[]`, no warning, the run proceeds as if the flags weren't there; every downstream "did the flag change anything" check reads vacuously clean | Godot splits engine args from USER args at a `--` (or `++`) separator; anything before it that the engine doesn't recognize is swallowed, and `get_cmdline_user_args()` only sees what comes AFTER the separator — `godot --path . scene.tscn -- --my-flag` works, `godot --path . scene.tscn --my-flag` silently doesn't |
| 73 | Reassigning a `PointLight2D`/canvas-light `texture` that has already been RENDERED emits `ERROR: Parameter "t" is null.` (from `texture_remove_from_decal_atlas`) once per replacement — the light keeps working, but a knob-driven rebuild (e.g. per `set_override`/slider tick) spams one error per change; the very first nil→texture assignment is silent, so boot never shows it | Replacing an already-ATLASED canvas-light texture removes the outgoing texture from the 2D-light decal atlas and that removal path trips the null-param check; render-only, so headless/tests are structurally blind (#21 family). Fix: keep the RID stable — write new pixels into the EXISTING `ImageTexture` via `update(img)` (same size/format) instead of assigning a fresh texture |
| 74 | Headless `--script` test: `ImageTexture.get_image()` returns the CREATION-time pixels forever — a later `update(img)` or even `set_image(img)` changes nothing in the readback, no error; any before/after pixel-diff assertion on an updated texture passes/fails vacuously | The dummy RenderingServer never uploads the new data, so readback serves the image captured at `create_from_image` time (sibling of #21/#59 — headless render-side blindness); assert on the source `Image`s (generator-level) or a build counter instead, and leave texture-content truth to a windowed run |
| 75 | An "is the mouse over UI?" gate via `get_window().gui_get_hovered_control() != null` (cursor swap, input gating, tooltip suppression) reads TRUE everywhere — over the world, over empty space, always — so the UI branch permanently wins; no error anywhere | In a pixel-viewport project the whole game view sits inside a full-screen `SubViewportContainer`, itself a Control with `mouse_filter=STOP` (required, to forward input to the inner viewport) — so it is ALWAYS the deepest hovered control when no real panel is under the cursor. Define over-UI as `hovered != null and not (hovered is SubViewportContainer)` (sibling of #49/#50) |
| 76 | `godot --headless --import` (or just opening the editor) in a checkout/worktree that is MISSING an *enabled* editor plugin's addon directory silently rewrites `project.godot` — strips that plugin from `[editor_plugins] enabled=` — surfacing as an unexpected `project.godot` diff; commit it and you ship a build config with the plugin disabled | On import/editor-init Godot tries to load every enabled plugin; a missing `res://addons/<x>/plugin.cfg` logs `Addon '…' failed to load. No directory found. Removing from enabled plugins.` and force-writes the pruned list back to `project.godot`. The class cache + headless suite still build green despite the absent addon, so nothing else flags it — `git checkout -- project.godot` before staging |

On an index-row match, read the entry's body file `gotchas/NN-<slug>.md` (NN = the row number zero-padded to 2 digits, e.g. row 13 -> `gotchas/13-classname-cache-reimport.md`) for the full entry — Symptom / Cause / Fix detail, proactive detection, commit hashes — before acting on the fix. The index alone is for routing, not for fixes.

## Adding new gotchas

**This skill is the SINGLE SOURCE for universal gotchas** — do not copy them into any project's
`docs/godot-gotchas.md`. Classify first:

- **Universal** — reproduces on any Godot project here given the same engine / tooling / addon
  (Godot, godot-ai, godot-mcp, GDScript, the headless harness, the dev machine, a third-party
  addon). **File it here** (steps below).
- **Project-local** — bound to one project's own code, scenes, assets, or param-tuning. File it in
  *that project's* `docs/godot-gotchas.md`, not here.
- A *convention* (axis-flip, naming, a design rule) is not a gotcha — record it as a `docs/adr/`
  entry, in neither catalog.

To file a universal gotcha here:

1. Append a row to the **Gotcha index** table with a one-line symptom and a one-line cause.
2. Create `gotchas/NN-<slug>.md` (NN = the new row number, zero-padded to 2 digits; slug = short
   kebab-case from the title) with **Symptom / Cause / Fix / Detect proactively / Confirmed by**
   subsections (same format as existing body files). A gotcha first hit in a specific project keeps
   its `Confirmed by: <project> <date>` anchor here — provenance lives in the skill.
3. Keep entries symptom-first — what you'd type into a search box at 11pm.
4. Do NOT renumber existing entries — the row number is the stable pointer from index to body file.
